/*
Each package/submodule in rpg-sage will use tsconfig.mono.json to ensure proper build order.
*/
{
	"extends": "../../../tsconfig.json",
	"compilerOptions": {
		"composite": true,
		"outDir": "./build",
		"rootDir": "./src",
	},
	"exclude": [
		"./test"
	],
	"include": [
		"./src"
	],
	"references": [
		// ensure dev-scripts are built first
		{ "path": "../../@rsc-utils/dev-scripts/tsconfig.mono.json" },
	],
}
