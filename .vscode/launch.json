{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Vitest Current File",
			"type": "node-terminal",
			"request": "launch",
			"command": "pnpm vitest --run --globals '${file}'",
			"cwd": "${workspaceFolder}"
		}
	]
}
